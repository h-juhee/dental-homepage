export function initScrollReveal() {
  const revealElements = document.querySelectorAll('[data-scroll-reveal]');
  if (revealElements.length === 0) return;

  // IntersectionObserver-based reveal behavior will be added later.
}
